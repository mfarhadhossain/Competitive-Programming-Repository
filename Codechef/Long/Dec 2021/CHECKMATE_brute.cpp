#include<bits/stdc++.h>
using namespace std;
#define endl "\n"
typedef long long int ll;
const int MAX = 1e5 + 10, MOD = 1e9 + 7;
bool bad(int row_king, int col_king, int  row_rook1, int  row_rook2, int  col_rook1, int  col_rook2){
	if(row_king == )
}
bool possible(int row_king, int col_king, int  row_rook1, int  row_rook2, int  col_rook1, int  col_rook2) {
	for(int r=row_rook1;r>=1;r--){
		if(r==row_rook2)break;

	}
}
void solve() {
	int row_king, col_king, row_rook1, row_rook2, col_rook1, col_rook2;
	cin >> col_king >> row_king;
	cin >> col_rook1 >> row_rook1;
	cin >> col_rook2 >> row_rook2;


}
int  main ()
{
	ios_base::sync_with_stdio(false);
	cin.tie(0);

	int t = 1; cin >> t;
	for (int tc = 1; tc <= t; tc++)
		solve();
	return 0;
}